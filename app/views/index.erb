<div class="row">
    <div class="col s12 m12 l3 offset-l3">
        <ul class="collapsible" data-collapsible="accordion">
            <% @place.each do |place| %>
                <li>
                  <div class="collapsible-header"><%= "#{place.name.capitalize}" %></div>
                  <div class="collapsible-body"><h6><%= "#{place.address}" %></h6></div>
                </li>
            <% end %>
        </ul>
    </div>
</div> -->
<div class="container">
    <div class="row">
    <div class="col s12 m12 l6">
        <% @posts.first(4).reverse.each do |post| %>
            <div class="card-panel">
                <div class="green-text row">
                    <%="#{post.place.name}" %>
                </div>
                <div class="content row">
                    <%="#{post.wait_time}" %>
                </div>
            </div>
            <% end %>
    </div>
    <div class="col s12 m12 l6">
        <% @place.first(4).reverse.each do |place| %>
            <div class="card-panel">
                <div class="green-text row">
                    <%="#{place.place.name}" %>
                </div>
                <div class="content row">
                    <%="#{place.wait_time}" %>
                </div>
            </div>
            <% end %>
    </div>
    </div>
</div>

<div class="container">
        <div class="card-panel col s12">
            <form action="/search/:search_term" method="POST">
                <div class="input-field">
                    <input id="search" name="search" type="search" required>
                    <label for="search"><i class="material-icons left">search</i>Search by Address</label>
                </div>
            </form>
        </div>
</div>